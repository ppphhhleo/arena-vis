interface:
  display_name: "Vis Arena"
  short_description: "Repo-specific patterns and workflows for vis-arena"
  default_prompt: "Use the vis-arena repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true